We're wrapping up this session. Please do the following:

1. Update `docs/plan.md`:
   - Check off every task completed this session
   - Update the `## Current state` section at the top (or create it if missing) with: today's date, current phase, what was just completed, and the exact next step to pick up from
   - Update `## Known issues` with any unresolved bugs or blockers — describe the problem, what was tried, and what to try next

2. Update `docs/decisions.md`:
   - Add any architectural or design decisions made this session that aren't already logged

3. Update `CLAUDE.md` if anything non-obvious was built that future sessions should know about

Then give me a one-line summary of where we are, so I can confirm it looks right before I close.
